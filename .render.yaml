services:
  - type: web
    name: capitalpay-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "./start.sh"
    envVars:
      - key: API_TOKEN
        sync: false
      - key: CHANNEL_ID
        value: "7279978383"
        sync: false
